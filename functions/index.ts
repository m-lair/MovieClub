require("module-alias/register");

import { comments, config, suggestions, memberships, movieClubs, users, posters } from "./src";

export { comments, config, suggestions, memberships, movieClubs, users, posters };
