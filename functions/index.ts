require("module-alias/register");

import { comments, config, suggestions, memberships, movieClubs, movies, users, posters, commentNotifications } from "./src";

export { comments, config, suggestions, memberships, movieClubs, movies, users, posters, commentNotifications };
