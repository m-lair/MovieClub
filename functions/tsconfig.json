{
  "compilerOptions": {
    "module": "ESNext",                         // Specifies the module code generation: 'ESNext' for the latest ECMAScript module standard.
    "moduleResolution": "node",                 // Determines how modules get resolved; 'node' mimics Node.js resolution.
    "esModuleInterop": true,                    // Enables emit interoperability between CommonJS and ES Modules, allowing default imports from CommonJS.
    "allowSyntheticDefaultImports": true,       // Allows default imports from modules without a default export.
    "resolveJsonModule": true,                  // Enables importing JSON files as modules.
    "target": "ES2020",                         // Specifies the output language level; ES2020 includes the latest features.
    "lib": ["ESNext"],                          // Specifies library files to include in the compilation; 'ESNext' includes the latest standard library features.
    "skipLibCheck": true,                       // Skips type checking of declaration files (.d.ts) for faster builds.
    "outDir": "./build",                         // Redirects output files to the specified directory (e.g., ./dist).
    "allowJs": true,                            // Allows JavaScript files to be included and transpiled alongside TypeScript files.
    "checkJs": true,                            // Optionally checks JavaScript files for type errors, helping during the migration to TypeScript.
    "baseUrl": "./",                            // Set base URL to the root of your project
    "paths": {                                  // Define aliases here
      "src": ["src"],
      "utilities": ["src/utilities"],           
      "index": ["index"],
      "firestore": ["src/firestore"],
      "test": ["test"],
      "mocks": ["test/mocks"]
    }
  },
  "include": ["./src/**/*"],                    // Includes all files in the 'src' directory and its subdirectories for compilation.
  "exclude": ["node_modules"]                   // Excludes the 'node_modules' directory from the compilation process to improve performance.
}